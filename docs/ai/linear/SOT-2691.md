# SOT-2691 — 分析成果物クロス参照（cycle8 C2）

- URL: https://linear.app/sota-dev/issue/SOT-2691
- Parent: SOT-2689（cycle8）
- Target repo: /workspaces/signate-messy-drive-rag
- Branch: feat/sot-2691-analysis-xref
- Flag: `RAG_ANALYSIS_XREF`（既定 OFF ⇒ byte-identical）

## 対象 idx と設計
- **idx60**（document_extract, gold=AI-05、AI-08、AI-09）: 最終報告の「未完事項/要アクション（未完事項）」
  節スコープで管理ID抽出。案件束縛=glossary.company_of（部分名正規化を内包）。→ MATCH 見込み。
- **idx73**（enum_set, gold=Gender）: train.csv カテゴリ列 nunique × features.py `MAX_CATEGORICAL_UNIQUE`(100)
  で閾値未満のカテゴリ列。かえで=Gender(2<100)。→ MATCH 見込み。
- **idx53**（stretch, derived_calculation, gold=6）: 最終報告「選択特徴量」節 − train.csv 原列 = ENG-FT 数。
  東都=6。→ MATCH 見込み。
- **idx36**（fact_lookup, gold=0.09619112771492555）: 中間段階(T04 linear)のフル精度 F1 は成果物に
  丸め(leaderboard 8桁 0.73296712)しか無く、フル精度 gold と ~1e-9 ずれ judge Incorrect（SOT-2687 実測）。
  → **未配線・honest abstain**（正解を焼けない問いは wrong を作らない）。staged_metrics ストアには最終F1
  フル精度を honest 併記。

## 実装
- build: `src/rag/index/analysis_xref_store.py`（全案件横断・LLM非使用・べき等）
- serve: `src/rag/agent/analysis_xref_lane.py`（fact_layer.resolve 末尾に後置 + investigator tool）
- wiring: `src/rag/agent/fact_layer.py`（tool + resolve tail）
- tests: `tests/test_analysis_xref_store.py` / `tests/test_analysis_xref_lane.py`（offline 14件）
- gate: `scripts/sot2691_focused.sh`（TARGET=36,60,73,53 + 番兵10）

## 検証
- offline: 新規14件 pass、full suite **2091 passed**（16 warnings、既知の WMF warning のみ）
- store build: idx60=AI-05/08/09・idx73=Gender・idx53=6 決定論一致
- OFF byte-identical: lane None・tools() delta 0
- focused --dev（RAG_CLAUDE_MCP_RESUME=0 / RAG_FORBID_GEMINI=1）: **PASS**
  - target: idx60/73/53 = Perfect（deterministic）、idx36 = Missing（honest abstain）
  - sentinel: 10/10 MATCH、regressions=[]
  - 初回は live Sonnet の idx90 が Missing となり 9/10。1回のみ再試行し idx90=Perfect、10/10 を確認。
